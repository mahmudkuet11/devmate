#Features
- Create Virtual host on Nginx

#Supported Platform
- Ubuntu 64 bit

#Build
To build run the following commands sequentially.
- `npm install`
- `npm run prod`
- `npm run build`
- `npm run deb64`